1、知识点应用

   first（）是所选元素的第一个该元素

   append（）内部插入

2、编程思想

   把第一个li的高度变为0，插入到最后一个li的后面，然后利用以底为基准，设置高度将倒数第二个li顶上去

3、遇到的问题

   刚开始对ul定位以top为基准定的，导致最后一个li出现的效果是从上往下将高度撑开的

   而以bottom为基准时，最后一个li的高度是从下往上撑开的
